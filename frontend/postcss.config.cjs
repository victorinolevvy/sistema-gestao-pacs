// Arquivo renomeado para postcss.config.cjs para compatibilidade com "type": "module" no package.json
module.exports = {
  plugins: {
    // tailwindcss: {}, // Removido pois estamos usando Material UI
    autoprefixer: {},
  },
}